
/**
 * Namespace: MM
 * Only create <MM> namespace.
 */
EzMap = OpenLayers.Class({});

/**
 * Namespace: MM.Layer
 * Only create <MM.Layer> namespace.
 */
EzMap.Layer = OpenLayers.Class({});

/**
 * Namespace: MM.BaseType
 * Only create <MM.BaseType> namespace.
 */
EzMap.BaseType = OpenLayers.Class({});

/**
 * Namespace: MM.Format
 * Only create <MM.Format> namespace.
 */
EzMap.Format = OpenLayers.Class({});

/**
 * Namespace: MM.Control
 * Only create <MM.Control> namespace.
 */
EzMap.Control = OpenLayers.Class({});

/**
 * Namespace: MM.Control
 * Only create <MM.Control> namespace.
 */
EzMap.Strategy = OpenLayers.Class({});

